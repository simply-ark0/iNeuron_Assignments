#include<iostream>
#include<math.h>
using namespace std;


int main(){
    unsigned int l, b, h;
    cout<<"Enter length, breadth and height (spearated by spaces): ";
    cin>>l>>b>>h;

    cout<<"Area of Cuboid = "<<(l*b*h);

    return 0;

}